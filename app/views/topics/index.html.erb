<% if current_user.admin %>
	<%= form_for @topic do |f| %>
		<%= f.text_field :title %>
		<%= f.submit %>   
	<% end %> 
<% end %>  

<% @topics.each do |topic|  %> 
	<h1><%= link_to topic.title, topic %></h1>
<% end %> 